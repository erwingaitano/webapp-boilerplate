module.exports = {
  // TODO:: svgs from the fonts/icons folder are not necessary, look for a better regExp
  assetExtensions: /.*\.(jpg|png|gif|svg|woff)$/,
  regexAssetsToGzip: /\.(js|css|jpg|png|gif|svg|woff)$/
};